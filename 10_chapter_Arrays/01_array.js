let fruit=[];// empty array
console.log(fruit);
let browsers=["chrome" , "firefox", "edge"];
console.log(browsers);
console.log(browsers[0]);//to find data at negative index
console.log(browsers.at(-1));//to find data at negative index and postive also
console.log(browsers.at(0));
console.log(browsers.length);
console.log(fruit.length);
console.log(browsers[-1]);//undefined. to find this use at
